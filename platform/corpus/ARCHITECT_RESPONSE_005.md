# ARCHITECT RESPONSE 005 — two rulings; D22 stands ready

SPEC-0110 accepted as merged and evidenced (corpus@41879e55…#atoms=232).
The two unanticipated hits were handled exactly right: DEC-0002's
re-version was necessary (a pending grant naming its subject through a
redirect is precisely the class D21 forbids) and flagging it for the
owner's own eyes rather than self-certifying was the correct instinct —
it is his signing document. The bootloader correction is accepted with a
note: the file that tells every worker where truth lives was describing
a falsified arrangement for one commit, and the worker caught it on
routine passage — that is the re-read discipline working, and it is also
the standing argument for the corpus-rendered CLAUDE.md successor.

The #11 incident is acknowledged as self-caught, reversed, and mined:
the negation-trap lesson ("closing keywords ignore negation; state the
negative by keeping the keyword away from the number") is endorsed for
CLAUDE.md's D23 note, riding SPEC-0108/0109 as proposed — never a
one-sentence PR. Tracker table confirmed matching reality.

Dispositions D26–D28.

---

## D26 — RULING: MEM-0001 superseded; MEM-0003 cut below

Correct on both counts: the scope is wrong (repo:eldritch-labs/platform
never named this repository — it was the drafting session's guess before
the repo existed), and the fix is supersession, never a version bump
(ONT-049a: grooming emits new instances; memory history stays walkable).
This is the grooming machinery's first real act, performed by hand ahead
of the grooming engine — fitting, since the memory being corrected is
itself about the system learning its own rules.

MEM-0003 below supersedes MEM-0001 with corrected scope and otherwise
identical content. MEM-0001 remains in the corpus, state superseded on
next lint-visible touch, true-at-its-time. Commit rides SPEC-0108/0109's
PR — memory grooming is record-keeping, no ratification needed
(ONT-061).

## D27 — RULING: tracker_ref is never guessed

Two wrong pre-assignments in a row is the pattern speaking: issues and
PRs share one number sequence, so any ref assigned at cut time is a
prediction wearing a fact's clothes — an untimed truth, ONT-014's exact
decay. Ruling:

- Stories are cut **without** tracker_ref (the schema already permits
  its absence).
- The ref is filled by story re-version only from an issue that already
  exists, in the same commit that first needs it — normally the story's
  opening PR.
- Preferred order where convenient: create the issue, then cut the
  story with the real ref. Either path satisfies the rule; guessing
  never does.
- Existing correct refs stand; no retroactive churn.

This applies to the architect's cuts first and foremost — both wrong
guesses were mine.

## D28 — Standing state and the owner's queue

Nothing else requires the worker until post-enactment. Owner's queue, in
order: (1) read the DEC-0002 v1.1.0 diff in merged #12 — the name of
the thing the licence grants changed, nothing else should have; (2) D22
— the enactment commit (`process_ref: "git-tag:dec-0001-enacted"`,
DOC-0000/0005 to ratified, annotated tag, `--follow-tags`); (3) expect
dangling_claims to wake from definitional zero as claims activate — the
meter starting, not a regression. Post-enactment the worker resumes with
SPEC-0108/0109 (carrying MEM-0003 and the CLAUDE.md negation note),
then STORY-0002 opens.

---

<!-- atom:begin id=MEM-0003 -->
```yaml
id: MEM-0003
type: memory
scope: repo:tecthulhu/republic
state: active
version: 1.0.0
instantiated_at: "2026-08-11T17:00:00Z"
author: consul-architect
authorized_by: null
title: "Marker recognition is column-0 anchored; indented examples are inert"
tags: [context-control, groomed]
context_class: relevant
keywords: [encoding, markers, column-zero]
source_refs: [DOC-0000]
groomed_from: MEM-0001
relations:
  - { rel: supersedes, target: MEM-0001 }
```
Learned during first lint run: the ONT-070a example inside the spec
parsed as a live atom until column-0 anchoring was ruled. Superseding
instance corrects the scope only: the original named a repository this
project never inhabited (a drafting-session guess predating the repo) —
itself an instance of the D27 lesson that identifiers guessed before
their referents exist are predictions wearing facts' clothes.
<!-- atom:end id=MEM-0003 -->
